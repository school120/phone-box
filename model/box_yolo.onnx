# 0) install
pip install ultralytics==8.2.103  # or latest stable

# 1) train
yolo detect train model=yolov8n.pt data=data.yaml imgsz=1280 epochs=60 batch=16 device=0
# tip: if you have fewer images, set epochs=100–150

# 2) export to ONNX (for the web)
yolo export model=runs/detect/train/weights/best.pt format=onnx opset=12 dynamic=True
# -> runs/detect/train/weights/best.onnx
